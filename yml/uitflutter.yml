# GitLab CI/CD Pipeline для Flutter проекта на macOS Runner
# 
# ✅ УПРАВЛЯЕМЫЕ PIPELINE ДЛЯ ВЕТКИ RELEASE (через переменные):
# - PIPELINE_TYPE="test": prepare → test → security_scan
# - PIPELINE_TYPE="android_release": build_android_release → deploy_google_play → notify_success_android  
# - PIPELINE_TYPE="ios_release": build_ios_release → deploy_app_store → notify_success_ios
# - PIPELINE_TYPE="all_release": build_android_release → deploy_google_play → build_ios_release → deploy_app_store → notify_success_all
# 
# ✅ WORKFLOW ДЛЯ ВЕТКИ MAIN:
# - prepare → test → security_scan (автоматически)
# - prepare_release, build_dev (manual jobs)
# - Каждый job выполняется строго после завершения предыдущего
# - При ошибке любого job весь pipeline останавливается
# 
# 📋 СТАДИИ:
# - prepare: подготовка окружения
# - test: тестирование и статический анализ
# - prepare_release: подготовка релиза (manual, после тестирования)
# - build_android, deploy_android: сборка и деплой Android
# - build_ios, deploy_ios: сборка и деплой iOS
# - post_deploy: дополнительные операции после релиза (manual jobs)
# - notify: уведомления о результатах
# 
# 🎯 ПОСЛЕДОВАТЕЛЬНОСТЬ ПО ПЛАТФОРМАМ:
# - ANDROID: build_android_release → deploy_google_play → notify_success_android (или notify_failure_android при ошибке)
# - iOS: build_ios_release → deploy_app_store → notify_success_ios (или notify_failure_ios при ошибке)
# 
# 🔐 iOS ПОДПИСЬ:
# - Автоматическая подпись через App Store Connect API
# - Используются APPLE_ID, APPLE_APP_ID и FASTLANE_APPLE_APPLICATION_SPECIFIC_PASSWORD
# - Не требуются сертификаты и provisioning profiles
# 
# 🔐 ANDROID ПОДПИСЬ:
# - Автоматическая подпись через keystore
# - Используются переменные ANDROID_KEYSTORE_*
# 
# 🚀 ДЕПЛОЙ:
# - iOS: TestFlight через fastlane pilot
# - Android: Google Play Production Track через fastlane supply

stages:
  - maintenance  # Стадия для служебных операций
  - prepare
  - test
  - prepare_release  # Подготовка релиза после тестирования
  - build_android
  - deploy_android
  - build_ios
  - deploy_ios
  - post_deploy  # Дополнительные операции после деплоя
  - notify

variables:
  FLUTTER_VERSION: "stable"
  # Кеширование для ускорения сборки
  PUB_CACHE: "$CI_PROJECT_DIR/.pub-cache"
  COCOAPODS_CACHE: "$CI_PROJECT_DIR/.cocoapods-cache"
  LANG: "en_US.UTF-8"
  LC_ALL: "en_US.UTF-8"
  # Оптимизация Git для проекта без submodules
  GIT_SUBMODULE_STRATEGY: none
  # Увеличиваем глубину для стабильности shallow clone
  GIT_DEPTH: 50
  
  # Общие функции для переиспользования
  SETUP_ENVIRONMENT: &setup_env |
    echo "🔧 Настройка окружения..."
    git config --global user.name "GitLab CI"
    git config --global user.email "ci@gitlab.com"
    export PATH="$PATH:$HOME/.pub-cache/bin"
    export PATH="$PATH:/opt/homebrew/bin"
  
  FLUTTER_CLEAN_AND_GET: &flutter_clean_get |
    echo "🧹 Очистка и подготовка Flutter окружения..."
    flutter clean
    flutter pub get
  
  IOS_SETUP: &ios_setup |
    echo "Подготовка iOS зависимостей..."
    (cd ios && pod install --repo-update)
    pwd # Убеждаемся что мы в корневой папке
  
  FASTLANE_CHECK: &fastlane_check |
    echo "🔧 Проверка fastlane..."
    if command -v fastlane &> /dev/null; then 
      echo "✅ Fastlane установлен"
      fastlane --version
    else 
      echo "❌ Fastlane не установлен!"
      echo "💡 Установите fastlane командой: brew install fastlane"
      echo "🔗 Документация: https://docs.fastlane.tools/getting-started/ios/setup/"
      exit 1
    fi
  
  TELEGRAM_NOTIFICATION: &telegram_notification |
    send_telegram_notification() {
      local message="$1"
      local platform="$2"
      
      echo "🔔 Проверка настроек Telegram уведомлений ($platform)..."
      
      if [ -n "$TELEGRAM_BOT_TOKEN" ]; then
        if [ -n "$TELEGRAM_CHAT_IDS" ]; then
          CHAT_LIST="$TELEGRAM_CHAT_IDS"
          echo "📢 Рассылка уведомления в чаты: $CHAT_LIST"
          
          IFS=',' read -ra CHAT_ARRAY <<< "$CHAT_LIST"
          SUCCESS_COUNT=0
          TOTAL_COUNT=${#CHAT_ARRAY[@]}
          
          for chat_id in "${CHAT_ARRAY[@]}"; do
            echo "📤 Отправка в чат: $chat_id"
            RESPONSE=$(curl -s -X POST "https://api.telegram.org/bot$TELEGRAM_BOT_TOKEN/sendMessage" \
              -d chat_id="$chat_id" \
              -d text="$message" \
              -d parse_mode="HTML")
            
            if echo "$RESPONSE" | grep -q '"ok":true'; then
              echo "✅ Успешно отправлено в чат: $chat_id"
              ((SUCCESS_COUNT++))
            else
              echo "❌ Ошибка отправки в чат $chat_id: $RESPONSE"
            fi
          done
          
          echo "📊 Результат рассылки: $SUCCESS_COUNT/$TOTAL_COUNT успешно"
        else
          echo "⚠️ Переменная TELEGRAM_CHAT_IDS не установлена"
          echo "💡 Для настройки уведомлений добавьте переменную в GitLab CI/CD Settings"
        fi
      else
        echo "⚠️ Переменная TELEGRAM_BOT_TOKEN не установлена"
        echo "💡 Для настройки уведомлений добавьте переменные в GitLab CI/CD Settings"
      fi
    }
  
  CHECK_VARIABLE: &check_variable |
    check_required_variable() {
      local var_name="$1"
      local var_value="$2"
      local help_message="$3"
      
      if [ -z "$var_value" ]; then
        echo "❌ Переменная $var_name не установлена!"
        echo "💡 $help_message"
        return 1
      fi
      return 0
    }
  
  GET_VERSION_INFO: &get_version_info |
    get_version_info() {
      VERSION=$(grep "const version = " lib/common/version.dart | cut -d"'" -f2 | tr -d '\r')
      BUILD_VERSION=$(grep "const buildVersion = " lib/common/version.dart | cut -d" " -f4 | cut -d";" -f1 | tr -d '\r')
      echo "Версия: $VERSION+$BUILD_VERSION"
    }


# Минимальный шаблон для macOS (только PATH и git config)
.macos_minimal: &macos_minimal
  image: macos-12-xcode-14
  tags:
    - mac
  before_script:
    - *setup_env

# Шаблон для macOS с Flutter подготовкой (без iOS)
.macos_flutter: &macos_flutter
  image: macos-12-xcode-14
  tags:
    - mac
  before_script:
    - *setup_env
    - *flutter_clean_get

# Шаблон для macOS с iOS подготовкой
.macos_ios: &macos_ios
  image: macos-12-xcode-14
  tags:
    - mac
  before_script:
    - *setup_env
    - *flutter_clean_get
    - *ios_setup

# Подготовка окружения
prepare:
  <<: *macos_flutter
  stage: prepare
  retry:
    max: 2
    when:
      - runner_system_failure
      - stuck_or_timeout_failure
  script:
    - echo "Подготовка окружения завершена"
    - flutter doctor -v
    - flutter pub deps
  artifacts:
    paths:
      - pubspec.lock
    expire_in: 1 hour
  rules:
    - if: $CI_COMMIT_REF_NAME == "main" && $CI_COMMIT_MESSAGE !~ /\[skip ci\]/
    - if: $CI_COMMIT_REF_NAME == "release" && $PIPELINE_TYPE == "test"
    - if: $CI_MERGE_REQUEST_ID

# Статический анализ и тесты
test:
  <<: *macos_minimal
  stage: test
  needs:
    - prepare
  retry:
    max: 2
    when:
      - runner_system_failure
      - stuck_or_timeout_failure
  script:
    - echo "📊 Запуск статического анализа..."
    - flutter analyze --no-fatal-infos
    - echo "🧪 Запуск тестов..."
    - flutter test
    - echo "✨ Проверка форматирования..."
    - dart format lib/ test/
    - dart format --set-exit-if-changed lib/ test/
  rules:
    - if: $CI_COMMIT_REF_NAME == "main" && $CI_COMMIT_MESSAGE !~ /\[skip ci\]/
    - if: $CI_COMMIT_REF_NAME == "release" && $PIPELINE_TYPE == "test"
    - if: $CI_MERGE_REQUEST_ID


# Сборка для разработки (ветка main) - только вручную
build_dev:
  <<: *macos_ios
  stage: prepare
  script:
    - echo "Сборка development version..."
    - flutter build apk --debug
    - flutter build ios --debug --no-codesign
  artifacts:
    paths:
      - build/app/outputs/flutter-apk/*-debug.apk
      - build/ios/iphoneos/*.app
    exclude:
      - build/app/outputs/flutter-apk/*-release.apk
      - build/app/outputs/flutter-apk/*-profile.apk
    expire_in: 3 days
  only:
    - main
  when: manual

# Сборка Android Release
build_android_release:
  <<: *macos_minimal
  stage: build_android
  allow_failure: false
  before_script:
    - *setup_env
    - *check_variable
    - echo "🔧 Подготовка keystore для подписи..."
    - |
      check_required_variable "ANDROID_KEYSTORE_BASE64" "$ANDROID_KEYSTORE_BASE64" "Добавьте keystore в base64 формате в GitLab CI/CD Variables. См. документацию: docs/ANDROID_SIGNING_SETUP.md" || exit 1
      check_required_variable "ANDROID_KEYSTORE_PASSWORD" "$ANDROID_KEYSTORE_PASSWORD" "Добавьте пароль keystore в GitLab CI/CD Variables. См. документацию: docs/ANDROID_SIGNING_SETUP.md" || exit 1
      check_required_variable "ANDROID_KEY_ALIAS" "$ANDROID_KEY_ALIAS" "Добавьте алиас ключа в GitLab CI/CD Variables. См. документацию: docs/ANDROID_SIGNING_SETUP.md" || exit 1
      check_required_variable "ANDROID_KEY_PASSWORD" "$ANDROID_KEY_PASSWORD" "Добавьте пароль ключа в GitLab CI/CD Variables. См. документацию: docs/ANDROID_SIGNING_SETUP.md" || exit 1
      
      echo "✅ Все переменные для подписи установлены"
      echo "🔧 Создаем keystore из base64..."
      mkdir -p android/keystore
      echo "$ANDROID_KEYSTORE_BASE64" | base64 -d > android/keystore/release.keystore
      chmod 600 android/keystore/release.keystore
      echo "✅ Keystore создан"
  script:
    - echo "Сборка Android Release..."
    - echo "Настройка Android signing..."
    - |
      if [ -f "android/keystore/release.keystore" ]; then
        echo "✅ Keystore найден, настраиваем подпись..."
        echo "ANDROID_KEYSTORE_PASSWORD: ${ANDROID_KEYSTORE_PASSWORD:+***}"
        echo "ANDROID_KEY_ALIAS: ${ANDROID_KEY_ALIAS:+[СКРЫТО]}"
        echo "ANDROID_KEY_PASSWORD: ${ANDROID_KEY_PASSWORD:+***}"
      else
        echo "❌ Keystore не найден!"
        echo "Файл android/keystore/release.keystore не найден"
        echo "💡 Проверьте переменную ANDROID_KEYSTORE_BASE64 в GitLab CI/CD Variables"
        echo "📖 См. документацию: docs/ANDROID_SIGNING_SETUP.md"
        echo "⚠️  Сборка остановлена - подпись обязательна для релиза!"
        exit 1
      fi
    - flutter build apk --release --split-per-abi
    - flutter build appbundle --release
    - echo "📊 Размеры файлов:"
    - du -h build/app/outputs/bundle/*.aab 2>/dev/null || echo "AAB файлы не найдены"
    - echo "📁 Содержимое папки build/app/outputs/bundle/:"
    - ls -la build/app/outputs/bundle/ || echo "Папка не найдена"
    - echo "📁 Содержимое папки build/app/outputs/bundle/release/:"
    - ls -la build/app/outputs/bundle/release/ || echo "Папка не найдена"
    - echo "🔍 Поиск всех AAB файлов:"
    - find build -name "*.aab" -type f || echo "AAB файлы не найдены"
    - echo "🔐 Проверка подписи AAB файла..."
    - |
      if [ -f "build/app/outputs/bundle/release/app-release.aab" ]; then
        if jarsigner -verify -verbose -certs build/app/outputs/bundle/release/app-release.aab 2>/dev/null | grep -q "jar verified"; then
          echo "✅ AAB файл подписан корректно"
        else
          echo "⚠️  AAB файл не подписан или подпись некорректна"
          echo "💡 Проверьте настройки подписи в GitLab CI/CD Variables"
          echo "📖 См. документацию: docs/ANDROID_SIGNING_SETUP.md"
        fi
      else
        echo "❌ AAB файл не найден для проверки подписи"
      fi
  artifacts:
    paths:
      - build/app/outputs/bundle/release/app-release.aab
    expire_in: 1 week
  rules:
    - if: $CI_COMMIT_REF_NAME == "release" && $PIPELINE_TYPE == "android_release"
    - if: $CI_COMMIT_REF_NAME == "release" && $PIPELINE_TYPE == "all_release"

build_ios_release:
  <<: *macos_ios
  stage: build_ios
  allow_failure: false
  needs:
    - job: deploy_google_play
      optional: true
  before_script:
    - *setup_env
    - *flutter_clean_get
    - *ios_setup
    - *check_variable
  script:
    - |
      echo "📱 Сборка iOS приложения для TestFlight..."
      echo "📋 Информация о сборке:"
      echo "   Версия: $(grep '^version:' pubspec.yaml | sed 's/version: //' | sed 's/+.*//')"
      echo "   Номер сборки: $(grep '^version:' pubspec.yaml | sed 's/.*+//')"
      echo "   Дата сборки: $(grep 'buildDate' lib/common/version.dart | sed 's/.*buildDate = '\''//' | sed 's/'\'';.*//')"
      echo ""
      echo "🔐 Проверка переменных окружения..."
      check_required_variable "APPLE_ID" "$APPLE_ID" "Добавьте APPLE_ID в GitLab CI/CD Variables" || exit 1
      check_required_variable "FASTLANE_APPLE_APPLICATION_SPECIFIC_PASSWORD" "$FASTLANE_APPLE_APPLICATION_SPECIFIC_PASSWORD" "Добавьте FASTLANE_APPLE_APPLICATION_SPECIFIC_PASSWORD в GitLab CI/CD Variables" || exit 1
      check_required_variable "IOS_TEAM_ID" "$IOS_TEAM_ID" "Добавьте IOS_TEAM_ID в GitLab CI/CD Variables" || exit 1
      echo "✅ Все переменные окружения установлены"
      echo "📱 Подготовка к сборке iOS приложения..."
      echo "📱 Сборка iOS приложения..."
      # Получаем версию и номер сборки из pubspec.yaml
      VERSION=$(grep '^version:' pubspec.yaml | sed 's/version: //' | sed 's/+.*//' | sed 's/#.*//' | xargs)
      BUILD_NUMBER=$(grep '^version:' pubspec.yaml | sed 's/.*+//' | sed 's/#.*//' | xargs)
      echo "📋 Используемая версия: $VERSION"
      echo "📋 Номер сборки: $BUILD_NUMBER"
      flutter build ios --release --build-name=$VERSION --build-number=$BUILD_NUMBER
      echo "📦 Создание архива..."
      cd ios
      xcodebuild -workspace Runner.xcworkspace -scheme Runner -configuration Release -archivePath build/Runner.xcarchive -destination generic/platform=iOS archive
      echo "📤 Экспорт IPA..."
      xcodebuild -exportArchive -archivePath build/Runner.xcarchive -exportOptionsPlist exportOptions.plist -exportPath build/
      cd ..
      echo "✅ iOS сборка завершена"
      echo "📊 Размер IPA: $(find ios/build -name '*.ipa' -exec ls -lh {} \; | awk '{print $5}' | head -n1)" || echo "IPA файл не найден"
  artifacts:
    paths:
      - build/ios/iphoneos/*.app
      - build/ios/ipa/*.ipa
      - ios/build/*.ipa
      - ios/build/*.ipa
    expire_in: 1 week
  rules:
    - if: $CI_COMMIT_REF_NAME == "release" && $PIPELINE_TYPE == "ios_release"
    - if: $CI_COMMIT_REF_NAME == "release" && $PIPELINE_TYPE == "all_release"

# Параллельная сборка для обеих платформ (ручная)
build_all_platforms:
  <<: *macos_ios
  stage: post_deploy
  parallel:
    matrix:
      - PLATFORM: ["android", "ios"]
  script:
    - echo "Параллельная сборка для $PLATFORM..."
    - |
      if [ "$PLATFORM" = "android" ]; then
        echo "Сборка Android..."
        flutter build apk --release --split-per-abi
        flutter build appbundle --release
      elif [ "$PLATFORM" = "ios" ]; then
        echo "Сборка iOS..."
        export DEVELOPER_TEAM_ID="$IOS_TEAM_ID"
        # Получаем версию и номер сборки из pubspec.yaml
        VERSION=$(grep '^version:' pubspec.yaml | sed 's/version: //' | sed 's/+.*//' | sed 's/#.*//' | xargs)
        BUILD_NUMBER=$(grep '^version:' pubspec.yaml | sed 's/.*+//' | sed 's/#.*//' | xargs)
        flutter build ios --release --build-name=$VERSION --build-number=$BUILD_NUMBER
        
        # Создаем IPA файл
        mkdir -p build/ios/ipa
        APP_FILE=$(find build/ios/iphoneos -name "*.app" -type d | head -n 1)
        if [ -n "$APP_FILE" ]; then
          APP_NAME=$(basename "$APP_FILE")
          IPA_NAME=$(echo "$APP_NAME" | sed 's/\.app$//')
          cd build/ios/iphoneos
          mkdir -p Payload
          cp -R "$APP_NAME" Payload/
          zip -r "../ipa/$IPA_NAME.ipa" Payload/
          rm -rf Payload
          cd ../../..
        fi
      fi
  artifacts:
    paths:
      - build/app/outputs/bundle/release/app-release.aab
      - build/ios/iphoneos/*.app
      - build/ios/ipa/*.ipa
    expire_in: 1 week
  only:
    - release
  when: manual



deploy_google_play:
  <<: *macos_minimal
  stage: deploy_android
  needs:
    - build_android_release
  allow_failure: false
  before_script:
    - *setup_env
    - *fastlane_check
    - *check_variable
  script:
    - |
      echo "\n=== [Google Play Deploy] Начало загрузки релиза ===\n"
      
      # Проверка наличия AAB файла
      if [ ! -f "build/app/outputs/bundle/release/app-release.aab" ]; then
        echo "❌ AAB файл не найден!"
        echo "📁 Содержимое папки build/app/outputs/bundle/:"
        ls -la build/app/outputs/bundle/ || echo "Папка не найдена"
        echo "📁 Содержимое папки build/app/outputs/bundle/release/:"
        ls -la build/app/outputs/bundle/release/ || echo "Папка не найдена"
        echo "💡 Пропускаем деплой в Google Play"
        echo "⚠️  ВНИМАНИЕ: Джоб завершается с ошибкой, так как AAB файл не найден!"
        exit 1
      fi
      
      echo "✅ AAB файл найден: build/app/outputs/bundle/release/app-release.aab"
      echo "📊 Размер AAB файла: $(du -h build/app/outputs/bundle/release/app-release.aab | cut -f1)"
      
      # Проверка переменных окружения
      check_required_variable "GOOGLE_PLAY_SERVICE_ACCOUNT_JSON" "$GOOGLE_PLAY_SERVICE_ACCOUNT_JSON" "Добавьте JSON ключ сервисного аккаунта в GitLab CI/CD Variables. См. документацию: docs/GOOGLE_PLAY_SETUP.md" || exit 1
      check_required_variable "PACKAGE_NAME" "$PACKAGE_NAME" "Добавьте package name приложения в GitLab CI/CD Variables. См. документацию: docs/GOOGLE_PLAY_SETUP.md. Пример значения: com.example.yourapp" || exit 1
      
      # Проверка соответствия PACKAGE_NAME с applicationId
      # ВНИМАНИЕ: Замените на ваш реальный Package Name
      EXPECTED_PACKAGE_NAME="com.example.yourapp"
      if [ "$PACKAGE_NAME" != "$EXPECTED_PACKAGE_NAME" ]; then
        echo "⚠️  ВНИМАНИЕ: PACKAGE_NAME ($PACKAGE_NAME) не совпадает с applicationId ($EXPECTED_PACKAGE_NAME)"
        echo "💡 Убедитесь, что в Google Play Console зарегистрировано приложение с правильным Package Name"
        echo "📖 См. документацию: docs/GOOGLE_PLAY_SETUP.md"
      fi
      
      echo "✅ Переменные окружения проверены"
      echo "📦 Package Name: [СКРЫТО]"
      
      # Создание JSON файла сервисного аккаунта (без вывода в логи)
      echo "$GOOGLE_PLAY_SERVICE_ACCOUNT_JSON" > google-play-service-account.json 2>/dev/null
      
      # Проверка валидности JSON
      if ! jq empty google-play-service-account.json 2>/dev/null; then
        echo "❌ Неверный формат JSON в GOOGLE_PLAY_SERVICE_ACCOUNT_JSON!"
        echo "💡 Проверьте формат JSON ключа"
        echo "📖 См. документацию: docs/GOOGLE_PLAY_SETUP.md"
        rm -f google-play-service-account.json
        exit 1
      fi
      
      echo "✅ JSON ключ сервисного аккаунта валиден"
      
      # Проверка содержимого JSON (без вывода чувствительных данных)
      CLIENT_EMAIL=$(jq -r '.client_email' google-play-service-account.json 2>/dev/null)
      PROJECT_ID=$(jq -r '.project_id' google-play-service-account.json 2>/dev/null)
      
      if [ -n "$CLIENT_EMAIL" ] && [ -n "$PROJECT_ID" ]; then
        echo "✅ Сервисный аккаунт и проект настроены корректно"
      else
        echo "❌ Не удалось извлечь данные из JSON ключа"
        rm -f google-play-service-account.json
        exit 1
      fi
      
      echo "Содержимое директории с .aab файлами:"
      ls -la build/app/outputs/bundle/release/
      
      echo "🚀 Запуск fastlane supply (загрузка .aab на Production Track)..."
      
      # Запуск fastlane для production трека (производственная среда)
      echo "📱 Загрузка на Production Track (производственная среда)..."
      if fastlane supply \
        --aab build/app/outputs/bundle/release/app-release.aab \
        --package_name "$PACKAGE_NAME" \
        --json_key google-play-service-account.json \
        --track production \
        --skip_upload_metadata true \
        --skip_upload_images true \
        --skip_upload_screenshots true \
        --skip_upload_changelogs true \
        --validate_only false \
        --release_status draft; then
        echo "✅ fastlane supply завершён успешно. .aab загружен на Production Track."
        echo "📱 Приложение загружено в черновик и готово к публикации"
        echo "💡 Для публикации перейдите в Google Play Console → Production"
      else
        echo "❌ ОШИБКА: fastlane supply завершился с ошибкой!"
        echo "💡 Возможные причины:"
        echo "   - Неверный JSON ключ сервисного аккаунта"
        echo "   - Приложение не создано в Google Play Console"
        echo "   - Неверный Package Name"
        echo "   - Проблемы с сетью или серверами Google"
        echo "📖 См. документацию: docs/GOOGLE_PLAY_SETUP.md"
        rm -f google-play-service-account.json
        exit 1
      fi
      
      # Очистка
      rm -f google-play-service-account.json
      
      echo "\n=== [Google Play Deploy] Завершено ===\n"
  rules:
    - if: $CI_COMMIT_REF_NAME == "release" && ($PIPELINE_TYPE == "android_release" || $PIPELINE_TYPE == "all_release")
      allow_failure: false

# Уведомления в Telegram об успешном Android релизе
notify_success_android:
  <<: *macos_minimal
  stage: notify
  needs:
    - deploy_google_play
  script:
    - *get_version_info
    - *telegram_notification
    - |
      get_version_info
      MESSAGE="✅ Android релиз $VERSION+$BUILD_VERSION успешно загружен!%0A%0A🤖 Ветка: $CI_COMMIT_REF_NAME%0A📱 Коммит: $CI_COMMIT_SHA%0A👤 Автор: $GITLAB_USER_NAME%0A%0A🤖 Android: Приложение загружено в Google Play Production Track"
      send_telegram_notification "$MESSAGE" "Android релиз"
  rules:
    - if: $CI_COMMIT_REF_NAME == "release" && $PIPELINE_TYPE == "android_release"
  allow_failure: true

deploy_app_store:
  <<: *macos_minimal
  stage: deploy_ios
  needs:
    - build_ios_release
  allow_failure: false
  before_script:
    - *setup_env
    - *fastlane_check
    - *check_variable
  script:
    - |
      echo "\n=== [App Store Deploy] Начало загрузки в TestFlight ===\n"
      
      # Проверка наличия IPA файла будет выполнена ниже
      
      # Проверка переменных окружения
      check_required_variable "APPLE_ID" "$APPLE_ID" "Добавьте APPLE_ID в GitLab CI/CD Variables. Пример: your-apple-id@example.com" || exit 1
      check_required_variable "FASTLANE_APPLE_APPLICATION_SPECIFIC_PASSWORD" "$FASTLANE_APPLE_APPLICATION_SPECIFIC_PASSWORD" "Добавьте FASTLANE_APPLE_APPLICATION_SPECIFIC_PASSWORD в GitLab CI/CD Variables. Пример: xxxx-xxxx-xxxx-xxxx" || exit 1
      check_required_variable "APPLE_APP_ID" "$APPLE_APP_ID" "Добавьте APPLE_APP_ID (числовой ID приложения) в GitLab CI/CD Variables. Пример: 1234567890" || exit 1
      
      echo "✅ Переменные окружения проверены"
      echo "🍎 Apple ID: [СКРЫТО]"
      echo "📱 Apple App ID: [СКРЫТО]"
      echo "🔐 Проверка настроек fastlane..."
      echo "   FASTLANE_USER: ${FASTLANE_USER:+[УСТАНОВЛЕН]}"
      echo "   FASTLANE_PASSWORD: ${FASTLANE_PASSWORD:+[УСТАНОВЛЕН]}"
      echo "   FASTLANE_APPLE_APPLICATION_SPECIFIC_PASSWORD: ${FASTLANE_APPLE_APPLICATION_SPECIFIC_PASSWORD:+[УСТАНОВЛЕН]}"
      echo "   SPACESHIP_ONLY_ALLOW_INTERACTIVE_2FA: ${SPACESHIP_ONLY_ALLOW_INTERACTIVE_2FA:+[УСТАНОВЛЕН]}"
      
      echo "🚀 Запуск fastlane pilot (загрузка в TestFlight)..."
      
      # Настройка переменных окружения для fastlane
      export FASTLANE_USER="$APPLE_ID"
      export FASTLANE_PASSWORD="$FASTLANE_APPLE_APPLICATION_SPECIFIC_PASSWORD"
      export FASTLANE_APPLE_APPLICATION_SPECIFIC_PASSWORD="$FASTLANE_APPLE_APPLICATION_SPECIFIC_PASSWORD"
      export SPACESHIP_ONLY_ALLOW_INTERACTIVE_2FA=true
      export FASTLANE_SKIP_UPDATE_CHECK=true
      export FASTLANE_HIDE_CHANGELOG=true
      export FASTLANE_DISABLE_COLORS=true
      
      # Создание файла конфигурации fastlane для отключения интерактивности
      mkdir -p ~/.fastlane
      cat > ~/.fastlane/env << EOF
      FASTLANE_USER="$APPLE_ID"
      FASTLANE_PASSWORD="$FASTLANE_APPLE_APPLICATION_SPECIFIC_PASSWORD"
      FASTLANE_APPLE_APPLICATION_SPECIFIC_PASSWORD="$FASTLANE_APPLE_APPLICATION_SPECIFIC_PASSWORD"
      SPACESHIP_ONLY_ALLOW_INTERACTIVE_2FA=true
      FASTLANE_SKIP_UPDATE_CHECK=true
      FASTLANE_HIDE_CHANGELOG=true
      FASTLANE_DISABLE_COLORS=true
      EOF
      # Устанавливаем правильные права доступа для файла с секретами
      chmod 600 ~/.fastlane/env
      
      # Поиск IPA файла
      IPA_FILE=""
      # Ищем любой IPA файл в стандартных местах
      if [ -n "$(find ios/build -name '*.ipa' -type f 2>/dev/null)" ]; then
        IPA_FILE="$(find ios/build -name '*.ipa' -type f | head -n 1)"
      elif [ -n "$(find build/ios/ipa -name '*.ipa' -type f 2>/dev/null)" ]; then
        IPA_FILE="$(find build/ios/ipa -name '*.ipa' -type f | head -n 1)"
      elif [ -f "build/ios/ipa/Runner.ipa" ]; then
        IPA_FILE="build/ios/ipa/Runner.ipa"
      else
        echo "❌ IPA файл не найден ни в одном из ожидаемых мест!"
        echo "🔍 Поиск всех IPA файлов:"
        find . -name "*.ipa" -type f 2>/dev/null || echo "IPA файлы не найдены"
        exit 1
      fi
      
      echo "✅ Найден IPA файл: $IPA_FILE"
      echo "📊 Размер IPA файла: $(du -h $IPA_FILE | cut -f1)"
      
      # Финальная проверка переменных перед запуском
      echo "🔐 Финальная проверка переменных:"
      echo "   APPLE_ID: ${APPLE_ID:+[УСТАНОВЛЕН]}"
      echo "   APPLE_APP_ID: ${APPLE_APP_ID:+[УСТАНОВЛЕН]}"
      echo "   FASTLANE_APPLE_APPLICATION_SPECIFIC_PASSWORD: ${FASTLANE_APPLE_APPLICATION_SPECIFIC_PASSWORD:+[УСТАНОВЛЕН]}"
      echo "   FASTLANE_USER: ${FASTLANE_USER:+[УСТАНОВЛЕН]}"
      echo "   FASTLANE_PASSWORD: ${FASTLANE_PASSWORD:+[УСТАНОВЛЕН]}"
      echo "   SPACESHIP_ONLY_ALLOW_INTERACTIVE_2FA: ${SPACESHIP_ONLY_ALLOW_INTERACTIVE_2FA:+[УСТАНОВЛЕН]}"
      
      # Запуск fastlane pilot для загрузки в TestFlight
      if fastlane pilot upload \
        --ipa "$IPA_FILE" \
        --username "$APPLE_ID" \
        --apple_id "$APPLE_APP_ID" \
        --skip_waiting_for_build_processing true \
        --skip_submission true \
        --distribute_external false; then
        echo "✅ fastlane pilot завершён успешно. IPA загружен в TestFlight."
        echo "📱 Приложение доступно в TestFlight для тестирования"
      else
        echo "❌ ОШИБКА: fastlane pilot завершился с ошибкой!"
        echo "💡 Возможные причины:"
        echo "   - Неверный Apple ID или пароль приложения"
        echo "   - Приложение не создано в App Store Connect"
        echo "   - Проблемы с подписью кода"
        echo "   - Проблемы с сетью или серверами Apple"
        echo "📖 См. документацию: docs/APP_STORE_SETUP.md"
        exit 1
      fi
      
      # Очистка файла конфигурации с секретами
      rm -f ~/.fastlane/env
      
      echo "\n=== [App Store Deploy] Завершено ===\n"
  rules:
    - if: $CI_COMMIT_REF_NAME == "release" && ($PIPELINE_TYPE == "ios_release" || $PIPELINE_TYPE == "all_release")
      allow_failure: false

# Уведомления в Telegram об успешном iOS релизе
notify_success_ios:
  <<: *macos_minimal
  stage: notify
  needs:
    - deploy_app_store
  script:
    - *get_version_info
    - *telegram_notification
    - |
      get_version_info
      MESSAGE="✅ iOS релиз $VERSION+$BUILD_VERSION успешно загружен!%0A%0A🤖 Ветка: $CI_COMMIT_REF_NAME%0A📱 Коммит: $CI_COMMIT_SHA%0A👤 Автор: $GITLAB_USER_NAME%0A%0A🍎 iOS: Приложение доступно в TestFlight для тестирования"
      send_telegram_notification "$MESSAGE" "iOS релиз"
  rules:
    - if: $CI_COMMIT_REF_NAME == "release" && $PIPELINE_TYPE == "ios_release"
  allow_failure: true

# Автоматическое обновление версии
update_version:
  <<: *macos_ios
  stage: prepare
  script:
    - echo "Обновление версии..."
    - chmod +x scripts/update_version.sh
    - ./scripts/update_version.sh
    - |
      # Коммит изменений версии
      if [ -n "$(git status --porcelain)" ]; then
        git config user.name "GitLab CI"
        git config user.email "ci@gitlab.com"
        git add lib/common/version.dart pubspec.yaml
        git commit -m "ci: автоматическое обновление версии и даты сборки [skip ci]"
        git push origin $CI_COMMIT_REF_NAME
      fi
  only:
    - release
  when: manual

# Очистка старых артефактов и кеша
cleanup:
  <<: *macos_ios
  stage: maintenance  # Перемещено в стадию maintenance для служебных операций
  script:
    - echo "🔍 Анализ состояния системы..."
    - echo "Доступное место до очистки:"
    - df -h /
    
    - echo "📊 Размеры кешей до очистки:"
    - du -sh $PUB_CACHE || echo "PUB_CACHE не существует"
    - du -sh $COCOAPODS_CACHE || echo "COCOAPODS_CACHE не существует"
    - du -sh build/ || echo "build/ не существует"
    - du -sh ios/Pods/ || echo "ios/Pods/ не существует"
    - du -sh ~/Library/Developer/Xcode/DerivedData/ || echo "DerivedData не существует"
    
    - echo "🧹 Очистка временных файлов..."
    - flutter clean
    - cd ios && pod cache clean --all && cd ..
    
    - echo "🗑️ Удаление кешей..."
    # Принудительное завершение процессов Flutter
    - pkill -f "flutter" || true
    - pkill -f "dart" || true
    
    # Безопасное удаление с повторными попытками
    - |
      remove_with_retry() {
        local path="$1"
        local max_attempts=3
        local attempt=1
        
        while [ $attempt -le $max_attempts ]; do
          echo "Попытка $attempt из $max_attempts удалить $path"
          
          if [ ! -e "$path" ]; then
            echo "✅ Путь $path не существует, пропускаем"
            return 0
          fi
          
          if rm -rf "$path" 2>/dev/null; then
            echo "✅ Успешно удалено: $path"
            return 0
          else
            echo "⚠️ Ошибка при удалении $path"
            if [ $attempt -lt $max_attempts ]; then
              echo "Ожидание 5 секунд перед следующей попыткой..."
              sleep 5
            fi
          fi
          
          attempt=$((attempt + 1))
        done
        
        echo "❌ Не удалось удалить $path после $max_attempts попыток"
        return 1
      }
      
      # Удаление каждой директории с повторными попытками
      remove_with_retry "build/" || true
      remove_with_retry "$PUB_CACHE" || true
      remove_with_retry "$COCOAPODS_CACHE" || true
      remove_with_retry "ios/Pods/" || true
      remove_with_retry ~/Library/Developer/Xcode/DerivedData/ || true
      
      # Проверка результатов
      echo "📊 Проверка удаления:"
      for path in "build/" "$PUB_CACHE" "$COCOAPODS_CACHE" "ios/Pods/"; do
        if [ -e "$path" ]; then
          echo "⚠️ Внимание: $path все еще существует"
        else
          echo "✅ $path успешно удален"
        fi
      done
    
    - echo "✅ Очистка завершена"
    - echo "📊 Доступное место после очистки:"
    - df -h /
    - echo "💡 Освобождено места на диске"
  when: manual
  allow_failure: true

# Проверка безопасности
security_scan:
  <<: *macos_minimal
  stage: test
  needs:
    - test
  script:
    - dart run dependency_validator
  rules:
    - if: $CI_COMMIT_REF_NAME == "main" && $CI_COMMIT_MESSAGE !~ /\[skip ci\]/
    - if: $CI_COMMIT_REF_NAME == "release" && $PIPELINE_TYPE == "test"
    - if: $CI_MERGE_REQUEST_ID



# Ручная подготовка релиза: обновление версий и генерация changelog
prepare_release:
  <<: *macos_minimal
  stage: prepare_release
  variables:
    GIT_STRATEGY: clone
  before_script:
    - *setup_env
    - *check_variable
  script:
    - |
      echo "📝 Подготовка релиза через Makefile..."
      
      # Проверка наличия PAT для push в репозиторий
      check_required_variable "GITLAB_PAT" "$GITLAB_PAT" "Создайте Personal Access Token с правами write_repository и добавьте его в CI/CD Variables как GITLAB_PAT" || exit 1
      
      # Настройка Git для коммита и push
      git config --global user.name "GitLab CI"
      git config --global user.email "ci@${CI_SERVER_HOST}"
      git remote set-url origin "https://oauth2:${GITLAB_PAT}@${CI_SERVER_HOST}/${CI_PROJECT_PATH}.git"
      
      # Выполняем подготовку релиза через Makefile
      echo "🚀 Запуск make prepare-release..."
      make prepare-release
      
      # Создание коммита с обновлениями
      echo "🔄 Создание коммита с обновлениями..."
      # ВНИМАНИЕ: Убедитесь, что файлы существуют в вашем проекте
      git add pubspec.yaml lib/common/version.dart assets/changelog/changelog.html lib/feature/presentation/screen/splash_screen/bloc/splash_bloc.dart
      
      if ! git commit -m "release: Подготовка переменных к релизу [skip ci]"; then
        echo "❌ Ошибка при создании коммита"
        exit 1
      fi
      
      # Отправка изменений в репозиторий
      echo "🚀 Отправка изменений в репозиторий..."
      if ! git push origin HEAD:main; then
        echo "❌ Ошибка при отправке изменений в репозиторий"
        exit 1
      fi
      
      echo "✅ Все изменения успешно отправлены в репозиторий"
  artifacts:
    when: always
    paths:
      - pubspec.yaml
      - lib/common/version.dart
      - assets/changelog/changelog.html
    expire_in: 1 week
  only:
    - main
  when: manual

# Уведомления в Telegram об ошибке Android релиза
notify_failure_android:
  <<: *macos_minimal
  stage: notify
  script:
    - *telegram_notification
    - |
      MESSAGE="❌ Ошибка при публикации Android релиза!%0A%0A🤖 Ветка: $CI_COMMIT_REF_NAME%0A📱 Коммит: $CI_COMMIT_SHA%0A👤 Автор: $GITLAB_USER_NAME%0A%0A🤖 Платформа: Android%0A💡 Проверьте логи pipeline: $CI_PIPELINE_URL"
      send_telegram_notification "$MESSAGE" "ошибка Android релиза"
  rules:
    - if: $CI_COMMIT_REF_NAME == "release" && $PIPELINE_TYPE == "android_release"
      when: on_failure
  allow_failure: true

# Уведомления в Telegram об ошибке iOS релиза
notify_failure_ios:
  <<: *macos_minimal
  stage: notify
  script:
    - *telegram_notification
    - |
      MESSAGE="❌ Ошибка при публикации iOS релиза!%0A%0A🤖 Ветка: $CI_COMMIT_REF_NAME%0A📱 Коммит: $CI_COMMIT_SHA%0A👤 Автор: $GITLAB_USER_NAME%0A%0A🍎 Платформа: iOS%0A💡 Проверьте логи pipeline: $CI_PIPELINE_URL"
      send_telegram_notification "$MESSAGE" "ошибка iOS релиза"
  rules:
    - if: $CI_COMMIT_REF_NAME == "release" && $PIPELINE_TYPE == "ios_release"
      when: on_failure
  allow_failure: true

# Уведомления в Telegram об успешном All Release (Android + iOS)
notify_success_all:
  <<: *macos_minimal
  stage: notify
  needs:
    - deploy_app_store
  script:
    - *get_version_info
    - *telegram_notification
    - |
      get_version_info
      MESSAGE="✅ All Release $VERSION+$BUILD_VERSION успешно завершен!%0A%0A🤖 Ветка: $CI_COMMIT_REF_NAME%0A📱 Коммит: $CI_COMMIT_SHA%0A👤 Автор: $GITLAB_USER_NAME%0A%0A🤖 Android: Приложение загружено в Google Play Production Track%0A🍎 iOS: Приложение доступно в TestFlight для тестирования%0A%0A🚀 Обе платформы успешно опубликованы!"
      send_telegram_notification "$MESSAGE" "All Release - Android + iOS"
  rules:
    - if: $CI_COMMIT_REF_NAME == "release" && $PIPELINE_TYPE == "all_release"
  allow_failure: true

# Уведомления в Telegram об ошибке All Release
notify_failure_all:
  <<: *macos_minimal
  stage: notify
  script:
    - *telegram_notification
    - |
      MESSAGE="❌ Ошибка при публикации All Release!%0A%0A🤖 Ветка: $CI_COMMIT_REF_NAME%0A📱 Коммит: $CI_COMMIT_SHA%0A👤 Автор: $GITLAB_USER_NAME%0A%0A🔄 Платформы: Android + iOS%0A💡 Проверьте логи pipeline: $CI_PIPELINE_URL"
      send_telegram_notification "$MESSAGE" "ошибка All Release"
  rules:
    - if: $CI_COMMIT_REF_NAME == "release" && $PIPELINE_TYPE == "all_release"
      when: on_failure
  allow_failure: true 